<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CULTIVATION" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/brand_green</item>
        <item name="colorSecondary">@color/activeTagText</item>
        <item name="android:statusBarColor">@color/brand_green</item>
    </style>

    <style name="Theme.CULTIVATION" parent="Base.Theme.CULTIVATION" />
</resources>